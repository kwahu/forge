# Gra: Deepspire

Skrót dla katalogu FORGE — szczegóły w [`docs/GDD.md`](docs/GDD.md) i [`docs/MANIFEST.md`](docs/MANIFEST.md).

```bash
cd games/deepspire
npm install
npm test
npm run simulate -- 42 30
npm run playtest:golden
npm run server
# http://127.0.0.1:8788  (port: DEEPSPIRE_PORT)
```

Pakiet: `@forge/deepspire` (moduł **roguelike**). Katalog w planie nadrzędnym: **FORGE §6.5**.
